//Babel is a tool that transpiles modern JS code into an older version of JS that can run in any browser.
module.exports = {
  presets: [
    '@babel/preset-env',
    '@babel/preset-react',
  ],
 };
 